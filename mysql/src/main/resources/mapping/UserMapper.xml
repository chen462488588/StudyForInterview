<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mysql.mapper.UserMapper">

    <resultMap id="BaseResultMap" type="mysql.entity.User">
        <result column="id" jdbcType="INTEGER" property="id" />
        <result column="userName" jdbcType="VARCHAR" property="userName" />
        <result column="passWord" jdbcType="VARCHAR" property="passWord" />
        <result column="realName" jdbcType="VARCHAR" property="realName" />
        <result column="money" jdbcType="NUMERIC" property="money" />
    </resultMap>

    <select id="Sel" resultType="mysql.entity.User">
        select * from user where id = #{id}
    </select>

    <select id="getUsers" resultType="mysql.entity.User" >
        select * from user
    </select>
    <insert id="addUser" useGeneratedKeys="true"  keyProperty="id">
        insert into user(userName,passWord,realName,money) values (#{userName},#{passWord},#{realName},#{money})
    </insert>

    <update id="updateUser" >
        update user
    <set>
        <if test="userName!=null">userName=#{userName},</if>
        <if test="passWord!=null">passWord=#{passWord},</if>
        <if test="realName!=null">realName=#{realName},</if>
        <if test="money!=null">money=#{money}</if>
    </set>
            where id = #{id}
    </update>
</mapper>